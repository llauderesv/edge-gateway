service:
  name: ""
  namespace: default

backend:
  name: ""
  port: 80

gateway:
  name: edge-gateway
  namespace: envoy-gateway-system

route:
  hostnames: []
  path: /
  pathType: PathPrefix

rateLimit:
  enabled: false
  requests: 100
  unit: Minute

timeouts:
  enabled: false
  request: 15s

security:
  enabled: false
