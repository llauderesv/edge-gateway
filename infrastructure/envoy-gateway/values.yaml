# Parent-chart values (GatewayClass). Subchart config lives under gateway-helm.
gatewayClass:
  name: eg
  controllerName: gateway.envoyproxy.io/gatewayclass-controller
  # Optional: point at an EnvoyProxy for proxy customization
  # parametersRef:
  #   group: gateway.envoyproxy.io
  #   kind: EnvoyProxy
  #   name: eg
  #   namespace: envoy-gateway-system
  annotations: {}
