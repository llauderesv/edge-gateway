apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyProxy
metadata:
  name: {{ .Values.envoyProxy.name }}
  namespace: {{ .Release.Namespace }}
spec:
  provider:
    type: Kubernetes
    kubernetes:
      envoyDeployment:
        replicas: {{ .Values.envoyProxy.replicas }}
        patch:
          type: StrategicMerge
          value:
            metadata:
              labels:
                app.kubernetes.io/instance: {{ .Release.Name }}
              annotations:
                # argocd.argoproj.io/tracking-id: envoy-gateway:apps/Deployment:envoy-gateway-system/envoy-gateway
                argocd.argoproj.io/tracking-id: "{{ .Release.Name }}:apps/Deployment:envoy-gateway-system/{{ .Values.envoyProxy.name }}"
      envoyService:
        type: {{ .Values.envoyProxy.service.type }}
  telemetry:
    accessLog:
      settings:
      - sinks:
        - type: File
          file:
            path: /dev/stdout